import logo from './cart.png';
import '../componentes/logo.css'

function Logo() {
    return (
        <div>
             <img  src={logo} className='logo-carrito' />
        </div>
    )
}

export default Logo
